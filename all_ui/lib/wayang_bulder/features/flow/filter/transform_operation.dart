enum TransformOperation {
  filter, // Filter data based on condition
  map, // Transform each item
  aggregate, // Aggregate data (sum, count, etc.)
  groupBy, // Group data by field
  sort, // Sort data
  distinct, // Remove duplicates
}
