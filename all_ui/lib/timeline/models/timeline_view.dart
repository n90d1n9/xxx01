enum TimelineView { century, decade, year, month, day }

enum EventCategory {
  war,
  science,
  art,
  politics,
  technology,
  culture,
  exploration,
  nature,
  sports,
  religion,
  economics,
  invention,
}

enum SortMode { popularity, chronological, reverseChronological, relevance }

enum ViewMode { list, grid, timeline, map, graph }
