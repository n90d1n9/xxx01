enum Gender { male, female }

enum BloodType {
  aPositive,
  aNegative,
  bPositive,
  bNegative,
  abPositive,
  abNegative,
  oPositive,
  oNegative,
}

enum TimeFilter { day, month, semester, year }

enum EmploymentType { fullTime, partTime, contract, visiting }

enum EducationLevel { elementary, intermediate, secondary, higher }

enum StaffRole { admin, janitor, cook, other }

enum SubjectType { academic, islamic }

enum DifficultyLevel { beginner, intermediate, advanced }

enum AttendanceStatus { present, absent, late }

enum GradeType { exam, quiz, assignment }

enum MemorizationStatus { inProgress, completed, needsRevision, mastered }

enum IncidentSeverity { minor, major, critical }

enum DormitoryType { boys, girls, mixed }

enum PaymentStatus { pending, completed, failed }
