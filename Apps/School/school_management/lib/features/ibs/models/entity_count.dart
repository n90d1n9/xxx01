class EntityCount {
  final String name;
  final int count;
  final int change;
  final bool isPositive;

  EntityCount({
    required this.name,
    required this.count,
    required this.change,
    required this.isPositive,
  });
}
