[package]
name = "slide_engine"
version = "0.1.0"
edition = "2021"
description = "Presentation engine with an absolute positioning scene graph"

[dependencies]
rustc-hash = "1.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
