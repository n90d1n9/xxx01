[package]
name = "docs_engine"
version = "0.1.0"
edition = "2021"
description = "Rich text document engine with a block-based DOM structure"

[dependencies]

serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1.0", features = ["v4"] }
