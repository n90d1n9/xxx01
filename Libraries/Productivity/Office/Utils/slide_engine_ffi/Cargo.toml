[package]
name = "slide_engine_ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
slide_engine = { path = "../slide_engine" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
