[package]
name = "code_engine"
version = "0.1.0"
edition = "2021"
description = "Code editor engine with rope buffer, syntax highlighting and LSP"

[dependencies]
serde = { version = "1", features = ["derive"] }
waraq-core = { path = "../waraq" }
