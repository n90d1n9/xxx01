enum ComparisonOp {
  equals,
  notEquals,
  lessThan,
  lessOrEqual,
  greaterThan,
  greaterOrEqual,
  in_,
  notIn,
}

enum LogicalOp { and, or, not }

enum ArithmeticOp { add, subtract, multiply, divide, modulo }
