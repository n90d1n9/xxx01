class AdminShellMetadata {
  const AdminShellMetadata._();

  static const appName = 'Kaysir';
  static const companyName = 'Kayys Tech';
  static const version = 'v0.1.0';
  static const workspaceStatus = 'Workspace ready';

  static const privacySummary =
      'Kaysir keeps workspace data scoped to authorized operators and local business workflows.';
  static const termsSummary =
      'Use this workspace for approved commerce operations, tenant data, and account activity only.';
  static const supportSummary =
      'For workspace assistance, contact your Kaysir administrator or support desk.';
}
