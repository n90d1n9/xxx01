name: ky_builder_shared
description: Shared builder primitives and UI widgets for Kaysir visual builders.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter: {}
