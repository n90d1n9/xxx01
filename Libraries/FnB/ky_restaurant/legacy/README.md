# Legacy Restaurant Prototypes

These files are retained as reference material from earlier restaurant UI
experiments. The supported package API lives in `lib/src` and is exported by
`package:ky_restaurant/ky_restaurant.dart`.

The legacy prototypes are excluded from analysis because they depend on older
prototype-only packages and patterns.
