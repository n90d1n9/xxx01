import 'package:ky_fnb_core/ky_fnb_core.dart';

/// Backwards-compatible restaurant tag for acknowledged catalog readiness.
const restaurantCatalogReviewedTag = fnbMenuCatalogReviewedTag;

/// Backwards-compatible restaurant name for shared menu catalog entries.
typedef RestaurantMenuCatalogEntry = FnbMenuCatalogEntry;
