## 0.0.1

* Added reusable restaurant route metadata, operating snapshot models, seeded
  demo data, and a modular restaurant operations workspace screen.
* Moved earlier prototype screens to `legacy/` so the public package surface is
  analyzable and future modules can depend on it safely.
