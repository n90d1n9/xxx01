import 'package:flutter/material.dart';
import 'package:kays_otp/kays_otp.dart';

class Otp extends StatelessWidget {
  const Otp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: OtpPage(),
    );
  }
}